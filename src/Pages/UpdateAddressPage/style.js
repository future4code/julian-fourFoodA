import styled from 'styled-components';
import {
  TextField
} from '@material-ui/core';

export const UpdateAddressPageContainer = styled.div`
`
export const FormContainer = styled.form`
  display: flex;
  flex-direction: column;
  margin: 0 1em;
  > p {
    align-self: center;
    margin: 1em 0 0 0;
    padding: .75em;
  }
`